# Parte del Equipo — corrida 20260923-2101 (frente: todo)

**Resultado: 3 arreglos verificados y PUBLICADOS en producción (main `2cf5d918..4563ae91`, 4 commits, Netlify sirvió todo a los 10 s, 5/5 URLs clave en 200 y una evidencia servida por tarea). Duración: 21:01 → 22:45 (85 min; el límite de 60 se pasó porque T1 reescribe 691 páginas y cada commit corre el gate completo).**

Hola Héctor. Search Console (28 días): 15 111 impresiones (+120 %), 195 clics (+33 %), CTR 1.29 %, posición 8.7. La home sigue siendo lo que convierte ("electricista culiacan" posición 2.3, CTR 11.4 %). Hoy fui por la tarea que quedó pendiente el lunes (CSS móvil site-wide, que solo cabe si va primera) y por dos defectos mecánicos que tocaban cientos de páginas y que solo se pueden hacer con un fixer certificado.

## ✅ Arreglado y en producción (3), cada uno probado por un agente distinto del que lo hizo
1. **CSS móvil + hero más ligero** (T1, `d3a1bbda`): en las 3 hojas CSS: (a) red `img,picture,video{max-width:100%}` (40 de 42 imágenes de la home no tenían tope); (b) los testimonios pasan a 1 columna en móvil (a 320 px la tarjeta desbordaba 12 px); (c) `#main-content` con margen de scroll de 120 px, así el enlace "saltar al contenido" ya no aterriza debajo del menú fijo; (d) se quitó un `image-set()` del `.hero` que descargaba la foto del hero DOS veces en escritorio (la pinta el `<picture>`, el CSS era duplicado). Bump `?v=20260923` en 691 páginas + service worker v34. Verificado con puppeteer a 320/375/1280 en 5 páginas: mismas imágenes y tamaños que antes, 0 peticiones al webp duplicado. → https://electricistaculiacanpro.mx/ (CSS servido con las 3 reglas y sin image-set).
2. **Contraste del naranja en textos** (T2, `f8f91870`): 591 textos en 41 páginas (29 servicios, 11 blogs, gracias) usaban el naranja de marca `#E36414` como color de texto sobre fondo claro (3.3:1, falla el mínimo de accesibilidad 4.5:1). Fixer nuevo `brand-text-contrast-inline`: solo la propiedad `color` en estilos inline pasa a `#C2410C` (el naranja de texto del contrato de marca, 4.9:1 o más). Nada de títulos, descripciones, JSON-LD ni fondos. Barrido en las 41 páginas: 720 elementos, mínimo 4.88:1. → https://electricistaculiacanpro.mx/servicios/emergencia-24-7/ (las preguntas del FAQ ya en `#C2410C`).
3. **Un solo nombre de negocio en las 642 colonias** (T3, `bacff93e`): el JSON-LD de cada colonia decía `"Electricista Culiacán Pro - <Colonia>"` (642 nombres distintos para el mismo negocio y teléfono); la home y los 32 servicios dicen `"Electricista Culiacán Pro"`. Fixer nuevo `jsonld-name-colonia`; la zona sigue en `areaServed`. JSON-LD parsea 642/642 (las dos formas, @graph y lista), gate de página 29/29 en las indexables. → https://electricistaculiacanpro.mx/servicios/electricista-colonias-culiacan/tres-rios/
4. `4563ae91`: lastmod del sitemap 77/77 + índice al día (todas las URLs cambiaron hoy).

Verificación final (sonnet): 699 archivos en el diff (691 HTML + 3 CSS + sw.js + estado del bump + auto-fixers.py + 2 sitemaps), 0 borrados; los 2607 pares de líneas cambiadas en HTML clasificados en las 3 categorías esperadas (1374 bump + 642 name + 591 color, 0 fuera); ci-gate 0 ALTA; verify 691 mecánicos / 0 libres; paridad CSS total; gate por archivo 71/71; invariantes servidas en 691/691 (200, JSON-LD, canonical = og:url = twitter:url donde existe, 0 "plomero", email y teléfono correctos, 0 mojibake); precios de la página de precios idénticos (49 cifras); los 22 títulos/descripciones del experimento del 17-sep byte a byte iguales; hooks, checkers, tests y JS sin cambios.

## 🧭 Decisiones que tomé como coordinador
- **bk-7a9e9c01 (image-set duplicado) entró en T1**: tocaba la misma hoja servida y el bump ya se pagaba; con otro bump después habrían sido otras 691 páginas. Métrica: en producción a 1280 px solo se descarga el AVIF del `<picture>` (verificado en local; comprobar en `check-produccion.mjs` la próxima corrida).
- **T3 fue a las 642 colonias, no solo a las 29 indexables** (bk-18fc18d2 decía 29): las noindex también se sirven y Google las lee; el fixer es de texto, idempotente y lo certifica verify. Riesgo medio asumido, 0 fallos.
- **contacto/ se bumpeó a mano dentro de T1**: `_do_full_bump` lo salta por la cuarentena de auto-fixers, pero el check 40 exige el mismo `?v=` en todas las páginas y dejaba 1 ALTA que bloquea el pre-push; además contacto habría servido el CSS viejo desde caché immutable. Encolado bk-02768d84 para que el bump no aplique la cuarentena.
- **lastmod = fecha del último commit** (política del 16-sep): 77/77 a hoy porque todas cambiaron (T1 el `?v=`, T2/T3 contenido). El `dateModified` de los blogs no se tocó.
- **Backlog**: cerradas con commit bk-b1850812, bk-7a9e9c01, bk-8ef200da, bk-fea19f5a, bk-d4eae40f (T1), bk-18fc18d2 (T3), bk-42c08e0b (ya resuelta en 64d6a969); descartadas bk-28545186 (hooks intocable) y bk-6dd45feb (ya hecha el 21-sep, en medición); bloqueada bk-07361cc1 (ver abajo). Nuevas: bk-015c4cc4 (alt del logo sin tilde en 32 servicios, fixer), bk-02768d84 (bump vs cuarentena), bk-25e0574c (los generadores de colonias reintroducen el sufijo del name; solo importa si se reactivan).

## 🙋 Necesita tu decisión
- **Pedir la indexación de `/servicios/electricista-a-domicilio/` en Search Console** (Inspección de URL → Solicitar indexación) y quitar el sitemap fantasma `/sitemaps/servicios_colonias_sitemap.xml` (última descarga 8-jun, 1 error). Lleva meses "Descubierta, sin indexar, rastreo: nunca" pese a 50 enlaces desde páginas indexables y HTTP 200; no es contenido, y por MCP no se puede pedir. Sin eso, cualquier reescritura de esa página no la verá Google.
- **Regla del probador al prompt global**: el probador (haiku) volvió a aprobar por muestra (T2: 3 de 41 URLs; T3: 5 de 29) y alegó "sandbox" para no capturar pantalla cuando el probador de T1 sí lo hizo. Es la 3ª vez; según el protocolo la regla pasa en negritas al cuerpo de `~/.claude/agents/equipo-probador.md`, pero ese archivo está protegido y mi edición fue denegada. Te dejo el texto listo en EQUIPO.md → Lecciones → probador (primera línea en negritas): cópialo arriba de "Qué verificar" en el archivo global.
- **El "★4.8 / más de 150 reseñas" visible en ~619 páginas** (pendiente desde el 21-sep): dejarlo / "Calificación 4.8 en Google" con enlace / retirarlo con un fixer.
- **Precios visibles (33 páginas)** — sin cambios. T2 solo cambió el color de los `<td>` de la página de precios, no las cifras (49 = 49).
- **~720 archivos sin commit de Codex** en el árbol principal (rama `auto/diario-20260912-2001`): siguen ahí; EQUIPO.md, calificaciones y ESTADO de ese árbol están ATRASADOS respecto a main (hoy tuve que leerlos del worktree). Commitear y mergear tras ci-gate / descartar / guardar como patch.
- Eléctrico automotriz: 141 impresiones en 4 consultas; se ignora por NEGOCIO.md.

## ❌ Lo que no pude
- Nada quedó sin hacer del plan de 3 tareas. Descartes del pensador con evidencia: home y `/servicios/electricista/` no se tocan (en medición hasta el 20-oct; la home convierte al 11 %); canibalización blog-precios sin evidencia en GSC; CLS 0.73 en local es artefacto del servidor local (en producción 0.03); el CTA final "blanco sobre blanco" es una transición de 2.5 s; logo 140×70 en colonias (real 256×256) da CLS 0.046 (bajo el umbral).

## 📊 Calificaciones (8 reportes, 0 reprobados)
| Agente | Modelo | Reportes | Promedio /12 | Reprobados | Lecciones escritas | Cambio de modelo |
|---|---|---|---|---|---|---|
| equipo-pensador | opus | 1 | 12.0 | 0 | 0 (detectó que EQUIPO.md citaba un `check-skip-link.mjs` inexistente: corregido) | no |
| equipo-ejecutor | sonnet | 3 | 12.0 | 0 | 1 en EQUIPO.md (unit test propio antes del run --apply; regiones asignadas en archivo compartido) | no |
| equipo-arreglador | sonnet | 0 | — | — | no | no |
| equipo-probador | haiku (T2, T3) / sonnet (T1, FINAL) | 4 | 11.25 (haiku 10.5; sonnet 12.0) | 0 | 1 consolidada en negritas ("las N, no una muestra", 3ª vez) + pendiente de Héctor pasarla al prompt global | no (causa: instrucción, no capacidad) |

Lecciones del coordinador (4 en EQUIPO.md): (1) `git apply --cached --unidiff-zero` con un parche del árbol puso el bloque de T3 en la línea equivocada y el commit de T3 llevó un `auto-fixers.py` que no compilaba; lo cacé con `ast.parse` de HEAD antes de subir nada y lo amendé reconstruyendo el archivo con Python + `hash-object` (que era la lección del 21-sep). (2) Escribí calificaciones.jsonl después de briefear al ejecutor T1 (reincidencia del 17-sep). (3) `_do_full_bump` vs check 40. (4) 3 tareas con una site-wide = 85 min, no 60.

Costo aprox. de subagentes: ~0.70 M tokens (pensador 193k, ejecutores 254k, probadores 255k). Sin rondas de arreglador.

## 🚀 Publicación
Hecha: rama `equipo/20260923-2101` subida (pre-push: gate OK, "sin páginas HTML que indexar"); `git merge --ff-only` en main + `git push origin main` (`2cf5d918..4563ae91`). Producción comprobada 10 s después: HTTP 200 en /, emergencia-24-7, blog, contacto y tres-rios; CSS servido `?v=20260923` con `img,picture,video{max-width:100%}`, testimonios 1fr, `#main-content{scroll-margin-top:120px}` y 0 `image-set`; sw.js v34 con precache `?v=20260923`; emergencia con 6 `color:#C2410C` inline y 0 `color:var(--brand)`; tres-rios con `"name": "Electricista Culiacán Pro"` y areaServed "Tres Ríos, Culiacán"; sitemap 77/77 lastmod 2026-09-23; contacto con `?v=20260923`; 0 "plomero". Este parte, calificaciones, EQUIPO.md, backlog y ESTADO.md van en un commit `docs(equipo)` posterior en main.

## Bitácora
- 21:01 FASE 0: worktree limpio desde origin/main 2cf5d918 (rama equipo/20260923-2101); servidor 8097; worktree muerto del 22-sep (vacío) eliminado; calificaciones 21-sep leídas (13 reportes, 0 reprobados). EQUIPO.md y calificaciones leídos del worktree (el árbol principal está atrasado vs main). Health: ci-gate 0 ALTA, verify 0/0, sw v33, CSS ?v=20260801.
- 21:22 FASE 1: pensador (19 min, 193k) → 3 tareas, 4 a humano, 17 descartes con evidencia; 12/12. Decisiones: bk-7a9e9c01 entra en T1; T3 a las 642; sync-lastmod al cierre.
- 21:24–21:40 T1: ejecutor 12/12 (destapó check 40 vs cuarentena); contacto bumpeado a mano; cotejo propio 1374 líneas -/+ solo `?v=`; commit `d3a1bbda`. Error mío: calificaciones.jsonl escrito tras el briefing.
- 21:43–21:57 En paralelo: probador T1 (sonnet) PASA 13/13 con capturas; ejecutores T2 y T3 12/12 en regiones distintas de auto-fixers.py; cotejo propio de T2 (591 pares, 0 excepciones).
- 22:06 probador T3 PASA (gate por muestra 5/29: evidencia 2). Commit T3 con `apply --cached` mal posicionado (1d978070 no compilaba) → reconstruido y amend → `bacff93e`.
- 22:16 probador T2 PASA (puppeteer 3/41, capturas omitidas: evidencia 1). Barrido propio en las 41: 720 elementos, mínimo 4.88:1. Commit `f8f91870`.
- 22:27 sync-lastmod → `4563ae91`. FINAL (sonnet, 8 min) ok:true A-I sobre 691 páginas. Backlog cerrado/encolado; lecciones en EQUIPO.md.
- 22:41 push rama; merge ff + push main; producción verificada a los 10 s; servidor apagado.
